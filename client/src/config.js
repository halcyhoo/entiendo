const Config = {
  baseurl: "http://localhost:3001/", // The url the client resides at. Include trailing slash
  server: "http://localhost:3000/" // The url the server resides at. Include trailing slash
};

export default Config;
